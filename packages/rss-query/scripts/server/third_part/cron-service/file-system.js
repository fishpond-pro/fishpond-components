const fs = require('fs')
const path = require('path')

/**
 * 
 * @param {any} client 
 * @param {{ dir: string }} options 
 */
function syncFiles (client, options) {
  
}

exports.syncFiles = syncFiles
