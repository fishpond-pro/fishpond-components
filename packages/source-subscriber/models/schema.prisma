//
// @@tarat generated file
// provide by @tarat
// warning: auto generated by tarat.do not modifed this file
//
// original writing schema
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider        = "prisma-client-js"
  output          = "./customPrismaClient/client"
  previewFeatures = ["fullTextSearch", "fullTextIndex"]
}

// 自定义RPA，待开发

// auto composing schema 
model DataSource {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt

  type     Int // 0.RSShub 1.自定义爬虫脚本
  platform String? // 所属平台

  rss _RSS?
  rpa _RPA?
}

model RSS {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt

  name         String
  href         String // RSS地址
  scheduleCron String? // 定时任务

  dataSource _DataSource? @relation(fields: [dataSourceId], references: [id]) // 数据源
  dataSourceId Int? @unique
}

model RPA {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt
  name       String

  dataSource _DataSource? @relation(fields: [dataSourceId], references: [id]) // 数据源
  dataSourceId Int? @unique
}

model Polymita_message_timeline_Source {
  id              Int                                 @id @default(autoincrement())
  channel         String                              @unique // source url 或 rpa id
  lastUpdatedDate DateTime
  data            Polymita_message_timeline_Message[]

  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt
}

model Polymita_message_timeline_Message {
  id          Int                                @id @default(autoincrement())
  link        String
  title       String?
  time        DateTime?
  description String?
  type        String // article/video/audio
  content     Polymita_message_timeline_Content?
  source      Polymita_message_timeline_Source   @relation(fields: [sourceId], references: [id])
  sourceId    Int

  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt
}

model Polymita_message_timeline_Content {
  id          Int                               @id @default(autoincrement())
  title       String
  description String
  content     String // article content or media OSS URL
  message     Polymita_message_timeline_Message @relation(fields: [messageId], references: [id])
  messageId   Int                               @unique

  createdAt  DateTime @default(now())
  modifiedAt DateTime @default(now()) @updatedAt
}
