{
  "name": "fishpond-components",
  "scripts": {
    "build:all": "node scripts/release",
    "patch:all": "node scripts/patch",
    "release:all": "RELEASE=1 node scripts/release"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  }
}